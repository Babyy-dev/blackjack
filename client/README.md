# Vlackjack Client

React + Vite frontend for the Vlackjack casino experience.

## Setup

```bash
npm install
```

## Development

```bash
npm run dev
```

Set `VITE_API_BASE_URL` and `VITE_SOCKET_URL` in a `.env` file if your FastAPI or
Socket.IO server is not running at `http://localhost:8000`.
